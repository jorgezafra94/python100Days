"""
print exactly

A 'single quote' inside a double quote
A "double quote" inside a single quote
Alternatively you can just "escape" the quote

"""

print("A 'single quote' inside a double quote")  # when you have single quote use double quote to print
print('A "double quote" inside a single quote')  # when you have double quote use single quote to print
print("Alternatively you can just \"escape\" the quote")  # when you have single quote use double quote escaped to print
